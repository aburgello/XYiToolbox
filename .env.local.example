# Copy this file to `.env.local` and paste your keys in.
# `.env.local` is gitignored (the *.local rule), so nothing here is committed.
# Only used by scripts/bench-providers.cjs — the panel itself reads its key
# from the browser's own storage and never looks at this file.

ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
